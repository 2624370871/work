1、运行主程序
2、运行第六行代码，调用方法abc，将4,7,1分别传递给a，b，c
3、运行第十一行代码，方法abc接受第六行传递的值，分别存放在a，b，c中

3-1、运行第十二行代码，执行for循环，先定义变量i，将1赋值给i
3-2、判断i是否小于等于a，为真时就运行第十三行的嵌套循环中的第一个for循环，定义变量j，将1赋值给j
3-3、判断j是否小于b-i，为真时输出空格，然后执行j++，再判断j是否小于b-i，为真时就继续执行循环输出空格；直到判断为假,跳出嵌套的第一个for循环，执行嵌套中的第二个for循环
3-4、运行第十六行代码，定义变量j，将1赋值给j
3-5、判断j是否小于等于2*i-c，为真时输出*，然后执行j++，再判断j是否小于2*i-c，为真时就继续执行循环输出*；直到判断为假，跳出嵌套循环的第二个for循环，然后执行第十九行的代码，输出换行
3-6、再执行第十二行代码，执行i++，再判断i是否小于等于a，为真时继续执行循环体，直到判断为假，跳出for循环，同时释放a，b，c的内存

4、运行第七行代码，调用方法abc，将数值4,6，-1分别传递给a，b，c，执行第3步步骤

5、运行第八行代码，调用方法abc，将数值4,6，-3分别传递给a，b，c，执行第3步步骤

6，运行第九行代码，调用方法xyz，将5,4,6分别传递给x，y，z
7、运行第二十二行代码，方法xyz接受第六行传递的值，分别存放在x，y，z中
7-1、运行第二十三行代码，执行for循环，先定义变量i，将1赋值给i
7-2、判断i是否小于等于x，为真时就运行第二十四行的嵌套循环中的第一个for循环，定义变量j，将1赋值给j
7-3、、判断j是否小于y，为真时输出空格，然后执行j++，再判断j是否小于y，为真时就继续执行循环输出空格；直到判断为假,跳出嵌套的第一个for循环，执行嵌套中的第二个for循环
7-4、运行第二十七行代码，定义变量j，将1赋值给j
7-5、判断j是否小于等于z，为真时输出*，然后执行j++，再判断j是否小于z，为真时就继续执行循环输出*；直到判断为假，跳出嵌套循环的第二个for循环，然后执行第三十行的代码，输出换行
